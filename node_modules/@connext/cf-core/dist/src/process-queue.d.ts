import { Task } from "p-queue";
import { Node } from "./types";
export default class ProcessQueue {
    private readonly lockingService?;
    private readonly queues;
    constructor(lockingService?: Node.ILockService | undefined);
    addTask(lockNames: string[], task: Task<any>): Promise<any>;
    private getOrCreateLockQueue;
}
//# sourceMappingURL=process-queue.d.ts.map
export declare class MerkleTree {
    elements: string[];
    root: string;
    layers: string[][];
    constructor(_elements: string[]);
    readonly topLayer: string[];
    proof(element: string): string;
    verify(proof: string): boolean;
}

import { Signature } from "ethers/utils";
import { AppIdentity, NetworkContext, CFCoreTypes } from "../types";
import { EthereumCommitment } from "./types";
export declare class SetStateCommitment extends EthereumCommitment {
    readonly networkContext: NetworkContext;
    readonly appIdentity: AppIdentity;
    readonly hashedAppState: string;
    readonly appVersionNumber: number;
    readonly timeout: number;
    constructor(networkContext: NetworkContext, appIdentity: AppIdentity, hashedAppState: string, appVersionNumber: number, timeout: number);
    hashToSign(): string;
    getSignedTransaction(sigs: Signature[]): CFCoreTypes.MinimalTransaction;
    private getSignedStateHashUpdate;
}
//# sourceMappingURL=set-state-commitment.d.ts.map
import { Signature } from "ethers/utils";
import { CFCoreTypes } from "../types";
import { EthereumCommitment, MultisigTransaction } from "./types";
export declare abstract class MultisigCommitment extends EthereumCommitment {
    readonly multisigAddress: string;
    readonly multisigOwners: string[];
    constructor(multisigAddress: string, multisigOwners: string[]);
    abstract getTransactionDetails(): MultisigTransaction;
    getSignedTransaction(sigs: Signature[]): CFCoreTypes.MinimalTransaction;
    hashToSign(): string;
}
//# sourceMappingURL=multisig-commitment.d.ts.map
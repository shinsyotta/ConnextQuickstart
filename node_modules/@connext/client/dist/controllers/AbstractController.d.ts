import { CFCoreTypes } from "@connext/types";
import { providers } from "ethers";
import { ChannelProvider } from "../channelProvider";
import { ConnextClient } from "../connext";
import { Logger } from "../lib";
import { ConnextListener } from "../listener";
import { INodeApiClient } from "../node";
export declare abstract class AbstractController {
    name: string;
    connext: ConnextClient;
    log: Logger;
    node: INodeApiClient;
    channelProvider: ChannelProvider;
    listener: ConnextListener;
    ethProvider: providers.JsonRpcProvider;
    constructor(name: string, connext: ConnextClient);
    proposeAndInstallLedgerApp: (params: CFCoreTypes.ProposeInstallParams) => Promise<string>;
    proposeAndWaitForAccepted: (params: CFCoreTypes.ProposeInstallParams) => Promise<string>;
    private resolveInstall;
    private rejectInstall;
    private rejectProposal;
    private cleanupInstallListeners;
    private cleanupProposalListeners;
}
//# sourceMappingURL=AbstractController.d.ts.map
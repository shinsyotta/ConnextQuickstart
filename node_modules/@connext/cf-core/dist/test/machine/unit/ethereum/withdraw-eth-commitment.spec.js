"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const utils_1 = require("ethers/utils");
const ethereum_1 = require("../../../../src/ethereum");
describe("Withdraw ETH Commitment", () => {
    let commitment;
    let tx;
    const multisigAddress = utils_1.getAddress(utils_1.hexlify(utils_1.randomBytes(20)));
    const multisigOwners = [
        utils_1.getAddress(utils_1.hexlify(utils_1.randomBytes(20))),
        utils_1.getAddress(utils_1.hexlify(utils_1.randomBytes(20)))
    ];
    const to = utils_1.getAddress(utils_1.hexlify(utils_1.randomBytes(20)));
    const value = utils_1.bigNumberify(Math.round(10000 * Math.random()));
    beforeAll(() => {
        commitment = new ethereum_1.WithdrawETHCommitment(multisigAddress, multisigOwners, to, value);
        tx = commitment.getTransactionDetails();
    });
    it("should be to the receiver", () => {
        expect(tx.to).toBe(to);
    });
    it("should have the value being sent", () => {
        expect(tx.value).toEqual(value);
    });
});
//# sourceMappingURL=withdraw-eth-commitment.spec.js.map
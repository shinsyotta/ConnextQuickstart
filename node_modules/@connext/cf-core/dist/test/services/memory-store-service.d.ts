import { Node } from "@connext/types";
export declare class MemoryStoreService implements Node.IStoreService {
    private readonly delay;
    private readonly store;
    constructor(delay?: number);
    get(path: string): Promise<any>;
    set(pairs: {
        path: string;
        value: any;
    }[]): Promise<void>;
    reset(): Promise<void>;
}
export declare class MemoryStoreServiceFactory implements Node.ServiceFactory {
    private readonly delay;
    constructor(delay?: number);
    createStoreService(): MemoryStoreService;
}
//# sourceMappingURL=memory-store-service.d.ts.map
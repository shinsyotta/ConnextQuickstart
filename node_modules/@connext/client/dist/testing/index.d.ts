import { App, BigNumber } from "../types";
export declare const mkAddress: (prefix?: string) => string;
export declare const mkHash: (prefix?: string) => string;
export declare type SuccinctChannel<T = string | number | BigNumber> = {
    id: number;
    user: string;
    counterpartyXpub: string;
    multisigAddress: string;
    apps: App<T>[];
    updates: SuccinctChannelUpdate<T>[];
};
export declare type SuccinctChannelUpdate<T = string | number | BigNumber> = {
    id: number;
    freeBalance: [T, T];
    nonce: number;
    sig: [string, string];
    channel: SuccinctChannel<T>;
};
//# sourceMappingURL=index.d.ts.map
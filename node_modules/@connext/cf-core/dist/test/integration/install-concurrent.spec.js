"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const constants_1 = require("ethers/constants");
const utils_1 = require("ethers/utils");
const constants_2 = require("../../src/constants");
const bignumber_jest_matcher_1 = require("../machine/integration/bignumber-jest-matcher");
const setup_1 = require("./setup");
const utils_2 = require("./utils");
expect.extend({ toBeLt: bignumber_jest_matcher_1.toBeLt });
jest.setTimeout(7500);
const { TicTacToeApp } = global["networkContext"];
describe("Node method follows spec - install", () => {
    let multisigAddress;
    let nodeA;
    let nodeB;
    describe("Node A gets app install proposal, sends to node B, B approves it, installs it, " +
        "sends acks back to A, A installs it, both nodes have the same app instance", () => {
        beforeEach(async () => {
            const context = await setup_1.setup(global);
            nodeA = context["A"].node;
            nodeB = context["B"].node;
            multisigAddress = await utils_2.createChannel(nodeA, nodeB);
            await utils_2.collateralizeChannel(multisigAddress, nodeA, nodeB, utils_1.parseEther("2"));
        });
        it("install app with ETH", done => {
            let completedInstalls = 0;
            nodeB.on("PROPOSE_INSTALL_EVENT", (msg) => {
                utils_2.makeInstallCall(nodeB, msg.data.appInstanceId);
            });
            nodeA.on("INSTALL_EVENT", () => {
                completedInstalls += 1;
                if (completedInstalls === 2) {
                    done();
                }
            });
            const rpc = utils_2.makeProposeCall(nodeB, TicTacToeApp, undefined, constants_1.One, constants_2.CONVENTION_FOR_ETH_TOKEN_ADDRESS, constants_1.One, constants_2.CONVENTION_FOR_ETH_TOKEN_ADDRESS);
            nodeA.rpcRouter.dispatch(rpc);
            nodeA.rpcRouter.dispatch(rpc);
        });
    });
});
//# sourceMappingURL=install-concurrent.spec.js.map
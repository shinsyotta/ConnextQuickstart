import { ConnextInternal } from '../Connext';
import { AbstractController } from './AbstractController';
export declare const validateExchangeRate: (store: import("redux").Store<import("../state").ConnextState, import("redux").AnyAction>, rate: string) => string | undefined;
export declare class ExchangeController extends AbstractController {
    private static POLLER_INTERVAL_LENGTH;
    private poller;
    constructor(name: string, connext: ConnextInternal);
    start(): Promise<void>;
    stop(): Promise<void>;
    private pollExchangeRates;
    exchange: (toSell: string, currency: "token" | "wei") => Promise<any>;
}

import * as eth from 'ethers';
import { IConnextChannelOptions } from './Connext';
import { TransactionRequest, TransactionResponse } from './types';
export declare class Wallet extends eth.Signer {
    address: string;
    provider: eth.providers.BaseProvider;
    private signer?;
    private web3?;
    private external?;
    constructor(opts: IConnextChannelOptions);
    getAddress(): Promise<string>;
    signMessage(message: string): Promise<string>;
    private prepareTransaction;
    signTransaction(tx: TransactionRequest): Promise<string>;
    private signAndSendTransactionExternally;
    sendTransaction(txReq: TransactionRequest): Promise<TransactionResponse>;
}

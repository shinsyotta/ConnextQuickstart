import { BigNumber } from "ethers/utils";
import { CFCoreChannel, SwapParameters } from "../types";
import { AbstractController } from "./AbstractController";
export declare const calculateExchange: (amount: BigNumber, swapRate: string) => BigNumber;
export declare class SwapController extends AbstractController {
    private appId;
    swap(params: SwapParameters): Promise<CFCoreChannel>;
    private resolveInstallSwap;
    private rejectInstallSwap;
    private swapAppInstall;
    private cleanupInstallListeners;
}
//# sourceMappingURL=SwapController.d.ts.map
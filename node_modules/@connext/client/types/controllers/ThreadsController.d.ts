import { Address, ChannelState, Payment, ThreadHistoryItem, ThreadState } from '../types';
import { AbstractController } from './AbstractController';
export declare class ThreadsController extends AbstractController {
    openThread(receiver: Address, balance: Payment): Promise<{
        thread: ThreadState;
        channel: ChannelState;
    }>;
    closeThread(threadIndicator: ThreadHistoryItem): Promise<void>;
}

"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const constants_1 = require("ethers/constants");
const utils_1 = require("ethers/utils");
const constants_2 = require("../../src/constants");
const types_1 = require("../../src/types");
const bignumber_jest_matcher_1 = require("../machine/integration/bignumber-jest-matcher");
const setup_1 = require("./setup");
const utils_2 = require("./utils");
expect.extend({ toBeLt: bignumber_jest_matcher_1.toBeLt });
jest.setTimeout(7500);
const { TicTacToeApp } = global["networkContext"];
describe("Node method follows spec when happening concurrently - install virtual / uninstall", () => {
    let multisigAddressAB;
    let multisigAddressBC;
    let nodeA;
    let nodeB;
    let nodeC;
    let installedAppInstanceId;
    describe("NodeA can uninstall regular app with NodeB and install virtual app with nodeC concurrently", () => {
        beforeEach(async () => {
            const context = await setup_1.setup(global, true);
            nodeA = context["A"].node;
            nodeB = context["B"].node;
            nodeC = context["C"].node;
            multisigAddressAB = await utils_2.createChannel(nodeA, nodeB);
            multisigAddressBC = await utils_2.createChannel(nodeB, nodeC);
            await utils_2.collateralizeChannel(multisigAddressAB, nodeA, nodeB, utils_1.parseEther("2"));
            await utils_2.collateralizeChannel(multisigAddressBC, nodeB, nodeC, utils_1.parseEther("2"));
            const appDef = TicTacToeApp;
            installedAppInstanceId = await new Promise(async (resolve) => {
                nodeB.once(types_1.NODE_EVENTS.PROPOSE_INSTALL, (msg) => {
                    utils_2.makeInstallCall(nodeB, msg.data.appInstanceId);
                });
                nodeA.once(types_1.NODE_EVENTS.INSTALL, (msg) => {
                    resolve(msg.data.params.appInstanceId);
                });
                await nodeA.rpcRouter.dispatch(utils_2.makeProposeCall(nodeB, appDef, undefined, constants_1.One, constants_2.CONVENTION_FOR_ETH_TOKEN_ADDRESS, constants_1.One, constants_2.CONVENTION_FOR_ETH_TOKEN_ADDRESS));
            });
        });
        it("nodeA calls uninstall and install virtual", async (done) => {
            let completedActions = 0;
            nodeB.once(types_1.NODE_EVENTS.UNINSTALL, () => {
                completedActions += 1;
                if (completedActions === 2)
                    done();
            });
            nodeA.once(types_1.NODE_EVENTS.INSTALL_VIRTUAL, () => {
                completedActions += 1;
                if (completedActions === 2)
                    done();
            });
            nodeA.rpcRouter.dispatch(utils_2.constructUninstallRpc(installedAppInstanceId));
            utils_2.installVirtualApp(nodeA, nodeB, nodeC, TicTacToeApp);
        });
        it("separate nodes make the uninstall and install virtual calls", async (done) => {
            let completedActions = 0;
            nodeA.once(types_1.NODE_EVENTS.UNINSTALL, () => {
                completedActions += 1;
                if (completedActions === 2)
                    done();
            });
            nodeA.once(types_1.NODE_EVENTS.INSTALL_VIRTUAL, () => {
                completedActions += 1;
                if (completedActions === 2)
                    done();
            });
            nodeB.rpcRouter.dispatch(utils_2.constructUninstallRpc(installedAppInstanceId));
            utils_2.installVirtualApp(nodeA, nodeB, nodeC, TicTacToeApp);
        });
    });
});
//# sourceMappingURL=uninstall-install-virtual-concurrent.spec.js.map
import { Signature } from "ethers/utils";
import { CFCoreTypes } from "../types";
export declare abstract class EthereumCommitment {
    abstract hashToSign(signerIsIntermediary?: boolean): string;
    abstract getSignedTransaction(signatures: Signature[], intermediarySignature?: Signature): CFCoreTypes.MinimalTransaction;
}
export declare enum MultisigOperation {
    Call = 0,
    DelegateCall = 1,
    Create = 2
}
export declare type MultisigTransaction = CFCoreTypes.MinimalTransaction & {
    operation: MultisigOperation;
};
//# sourceMappingURL=types.d.ts.map